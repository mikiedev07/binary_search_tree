What's done here:

1) Building a binary search tree;
2) Search with inclusion in the binary search tree;
3) Implementation of algorithms for forward, backward and symmetric tree traversal;

*To perform a search, any traversal algorithm, you must first display the tree itself ("Show Tree" button)!